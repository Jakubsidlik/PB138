{
  "name": "pb138-studijni-plan",
  "version": "1.0.0",
  "description": "Webová aplikace pro organizaci lekcí, poznámek a studijních materiálů",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"cd server && bun run dev\" \"cd client && bun run dev\"",
    "build": "cd server && bun run build && cd ../client && bun run build",
    "preview": "cd client && bun run preview",
    "type-check": "cd server && bun run type-check && cd ../client && bun run type-check",
    "test": "bun test ./tests/unit ./tests/e2e/server"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "devDependencies": {
    "@clerk/testing": "^2.0.31",
    "@playwright/test": "^1.60.0",
    "@tanstack/router-vite-plugin": "*",
    "bun-types": "^1.3.14",
    "concurrently": "^8.2.2",
    "dotenv": "^17.4.2"
  },
  "dependencies": {
    "@tanstack/react-router": "^1.169.2",
    "lucide-react": "^1.11.0"
  }
}
